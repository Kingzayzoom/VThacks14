# Phase A visual review / 90 seconds

This is a frontend checkpoint, not the full four-minute sponsor-integration demo.

1. Open `/`. Inspect the wordmark, editorial heading and objective entry.
2. Click **Prepare a launch brief**; it fills the input without starting work. Click **Run objective** to create a labeled local demo mission.
3. Open **MISSIONS**, then select **Student product launch** in the mission selector and return to **FIELD**. This seeded scenario illustrates in-progress research/analysis and a missing capability.
4. Select Scout, Sage, Forge and Guardian. Inspect runtime state, identity evidence and narrow scopes separately. Switch Graph/List.
5. Click **Review request**. The drawer explicitly previews the missing visualization capability; no specialist is admitted in Phase A. Close with Escape.
6. Pause/resume the demo. Running edges stop/resume and the event rail records the command. The pending review stays pending.
7. Open the microphone control: voice is not configured and no audio is captured. **Use text instead** returns focus to the objective dock.
8. At 390px, inspect the accessible agent list and direct review button. Open navigation and settings to see the unconnected integration boundaries.

Stop for Roheen's visual approval. The full objective → recruitment → Guardian block → artifact demonstration belongs to Phase B. No current fixture should be presented as live ANS, voice or provider work.
